# SCRIPT — spec-intro

**Voice:** 中文男声，沉稳偏冷（NowVoice / nowvoice-tts skill；本会话出口策略拦截 nowvoice.ai，
待域名放行后生成）
**Voice settings:** 语速 0 · 音量 0 · 音调 0（先按默认出一版再校）
**Voice direction:** 像一个讲证据的工程师，不是广告旁白。克制、笃定、不煽情；
句末不上扬。红绿对比处（Frame 5）可略压低语速，收尾"是契约"要短促落地。

> 停顿标记 `(⏱️=Xs)` 已按 `nowvoice-tts` skill 的规则插好，**计入字符数、需原样提交**。
> 每条 Line 的缩进块即为送入 TTS 的正文，逐帧一一对应（Line N ↔ Frame N）。
> 生成后按 `<frame-id>.mp3` 命名放入 `audio/`，再跑 `audio.mjs sync-durations` 以真实时长覆盖估算。

---

## Line 1 — 问题 (Frame 1)

**Time:** 0.0 – 17.0s（估算）
**Delivery:** 开口即结论，"不慢"与"慢的是"形成对拍；"说不清凭什么"略加重。

    AI 写代码不慢。(⏱️=0.3s)慢的是回头确认——它做的到底是不是你要的。(⏱️=0.8s)需求散在聊天里，一路绿灯，却说不清凭什么。(⏱️=0.8s)动工之前，先把该定的定下来。

## Line 2 — 安装 (Frame 2)

**Time:** 0.0 – 13.5s（估算）
**Delivery:** 轻快利落，像在演示而不是推销。"一个都不碰"收干净。

    安装只有一行。(⏱️=0.4s)在你的项目里跑一句，三个命令就位。(⏱️=0.8s)它只写 .claude 目录，(⏱️=0.3s)你的代码和规格，一个都不碰。

## Line 3 — 三道门 (Frame 3)

**Time:** 0.0 – 20.5s（估算）
**Delivery:** 三段递进，每个命令名前微顿，让画面卡片跟上。
"没有中间层，就没有漂移"是本段的题眼，放慢半拍。

    三个命令，是三道门。(⏱️=0.5s)spec 把模糊的想法，变成能落地的规格。(⏱️=0.5s)build 按规格写码——计划每次重生成、从不留存，(⏱️=0.3s)没有中间层，就没有漂移。(⏱️=0.5s)yolo 是同一套规则的自动挡。

## Line 4 — 人机协同 (Frame 4)

**Time:** 0.0 – 18.0s（估算）
**Delivery:** "取舍、优先级、风险"三词分明、一词一拍，对齐画面弹出。
末句"直接说不行"要硬，不留余地。

    它不是听话的记录员。(⏱️=0.5s)能查的自己查，能定的自己定，理由写进决策日志。(⏱️=0.8s)只有取舍、优先级、风险才来问你，一次问清。(⏱️=0.8s)想法被证据否掉，它会直接说不行。

## Line 5 — 核心能力 (Frame 5)

**Time:** 0.0 – 20.0s（估算）
**Delivery:** 全片最冷静的一段。"能真的变红"对应画面两次泛红，略压语速；
"变不红的检查，本身就不算数"是全片最锋利的一句，字字清楚。

    底线只有一条：证据不能是空话。(⏱️=0.5s)每道承重的门，背后是一个能真的变红的探针。(⏱️=0.5s)变不红的检查，本身就不算数。(⏱️=0.8s)生成类成果还要过独立评审——(⏱️=0.3s)全新上下文，不许自己审自己。

## Line 6 — 效果收尾 (Frame 6)

**Time:** 0.0 – 19.5s（估算）
**Delivery:** 前半自信，"二十二条探针，全绿"可稍扬；
"抓出了它的自我夸大"要平——这是承认，不是表功。末两句断开，"是契约"短促收。

    它自己管着自己。(⏱️=0.5s)这个仓库的规格和探针，就是它跑出来的——(⏱️=0.3s)二十二条探针，全绿。(⏱️=0.8s)一次独立审计抓出了它的自我夸大，然后逐条改掉。(⏱️=0.8s)规格不是文档。(⏱️=0.4s)是契约。
