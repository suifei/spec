---
description: Implement the tasks of a Spec-Builder change, ticking checkboxes as you go.
argument-hint: "[change-name]"
---

Invoke the `spec-builder-apply` skill to implement the change's tasks.

Change name (optional): $ARGUMENTS
