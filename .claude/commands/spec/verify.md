---
description: Verify a Spec-Builder change's implementation against its artifacts (completeness/correctness/coherence).
argument-hint: "[change-name]"
---

Invoke the `spec-builder-verify` skill to verify the change implementation.

Change name (optional): $ARGUMENTS
