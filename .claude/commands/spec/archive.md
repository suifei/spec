---
description: Archive a completed Spec-Builder change (merge delta specs into living specs, then move to changes/archive/).
argument-hint: "[change-name]"
---

Invoke the `spec-builder-archive` skill to merge specs and archive a completed change.

Change name (optional): $ARGUMENTS
