---
description: Create or continue a Spec-Builder change and generate its artifacts (proposal, specs, design, tasks).
argument-hint: "<change-name | description | existing change to continue>"
---

Invoke the `spec-builder-propose` skill. Scaffold the workspace if needed, then
create the change and its artifacts (or continue a partial one).

Change name / description: $ARGUMENTS
