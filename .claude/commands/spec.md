---
description: Brainstorm to closure and write/update the project's authoritative SPEC.md (like /init, for the living spec).
argument-hint: "[new requirement, change, or anything to fold in — optional]"
---

Invoke the `spec` skill. Load the current `SPEC.md` and project state, reconcile
any drift, fold in the input below, brainstorm with me until every blocking
question is closed and every source of truth is pinned, record decisions, then
rewrite the complete `SPEC.md` and ensure `CLAUDE.md` points to it as the
highest-priority reference.

Input to fold in (optional): $ARGUMENTS
